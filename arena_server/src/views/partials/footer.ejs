<footer class="page-footer light-blue darken-3">
  <div class="row">
    <div class="col">
      <a href="https://github.com/afarago/SLCEE-2023-PC-public/#readme" class="breadcrumb" target="_blank"
        ><i class="material-icons"> help </i></a
      >
      2022-2023, Attila & Csaba
    </div>
    <div class="col right template" data-template="footer_info"></div>
  </div>
</footer>

<script>
  $(() => {
    const footer_info = Array.from(
      new Map([
        ['user', sessiondata?.name ?? sessiondata?.username],
        ['role', sessiondata?.role],
        ['db', sessiondata?.dbname],
        ['version', sessiondata?.version ]
      ]).entries()
    )
      .filter(([key, val]) => val)
      .map(([key, val]) => `${key}: ${val}`)
      .join(' · ');

    $('footer .template[data-template="footer_info"]').html(footer_info);
  });
</script>
